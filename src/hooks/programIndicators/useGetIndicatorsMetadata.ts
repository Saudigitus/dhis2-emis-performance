import { useGetProgramIndicators } from "./useGetProgramIndicators"

export const useGetIndicatorsMetadata = () => {
    const { getProgramIndicators } = useGetProgramIndicators()

    // const get

}